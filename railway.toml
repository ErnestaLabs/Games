[build]
dockerfilePath = "trading_games/Dockerfile"

[deploy]
startCommand = "python -m trading_games.agent_runner"
region = "asia-southeast1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[environments.production.variables]
PYTHONUNBUFFERED = "1"
DRY_RUN = "false"
